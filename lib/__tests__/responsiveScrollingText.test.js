const assert = require('assert');
const responsiveScrollingText = require('../index.js');

describe('responsiveScrollingText', () => {
  it('has a test', () => {
    assert(false, 'responsiveScrollingText should have a test');
  });
});
